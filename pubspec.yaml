name: spade_v4
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


 
  cupertino_icons: ^1.0.2
  # THIS IS PACKAGES FOR SPADE UI
  lite_rolling_switch: ^1.0.1
  custom_radio_grouped_button: ^2.2.0
  percent_indicator: ^4.2.3
  flutter_speed_dial: ^6.2.0
  # flutter_tinder_swipe: ^0.0.4
  flutter_svg_icons: ^0.0.1+3
  flutter_staggered_grid_view: ^0.6.2
  # stacked_list_carousel: ^1.0.0
  table_calendar: ^3.0.9
  flutter_slidable: ^3.0.0
  swipe_to: ^1.0.2
  gestures: ^1.0.0
  carousel_slider: ^4.0.0
  # flutter_svg: ^0.19.1
  flutter_svg: ^0.23.0+1
  swipe_cards: ^2.0.0+1
  animated_floating_buttons: ^0.0.1
  expendable_fab: ^0.0.3
  # PACKAGES FOR STATE MANAGEMENT
  equatable: ^2.0.5
  provider: ^6.0.5
  flutter_bloc: ^8.1.3
  #Google map packages
  google_maps_flutter: ^2.3.1
  geolocator: ^9.0.2
  geocoding: ^2.1.0
  #Progress indicator
  flutter_spinkit: ^5.2.0
  #logs
  logger: ^2.0.1
  #<<<<>>>>#
  camera: ^0.10.5+2
  video_player:
  audioplayers: ^5.0.0
  cached_network_image: ^3.2.3
  path_provider: ^2.0.15
  image_picker: ^1.0.2
  image_cropper: ^5.0.0
  emoji_picker_flutter: ^1.6.1
  giphy_get:
  uuid: ^3.0.7
  flutter_card_swiper: ^6.0.0
  hexagon: ^0.2.0
  get_it: ^7.6.0
  form_validator: ^2.1.1
  dio: ^5.3.3
  #<<<service>>>#
  http: ^1.1.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  rxdart: ^0.27.7
  flutter_native_splash:
  csc_picker: ^0.2.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter_native_splash:
  image: assets/images/onbaording.png
  color: "#f9f9f9"
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:

 
  uses-material-design: true

 
  assets:
     - assets/images/
     - assets/maptheme/nighttheme.json
     - assets/maptheme/darktheme.json
     - assets/maptheme/aubergine.json
     - assets/maptheme/retro.json
     - assets/maptheme/nightthemelabel.json
     - assets/discover/data.json
  
